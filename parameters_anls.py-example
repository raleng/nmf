# NMF Variables
features = 3
use_fcnnls = False
lambda_w = 100
lambda_h = 100

# Iteration/Algorithm Variables
max_iter = 10000
tol1 = 1e-5
tol2 = 1e-5

# File handling
load_file = 'data/pet-matlab/phantom2.mat'
load_var = 'sinodata1'
# load_var = 'sinodata'

save_name = 'nmf_anls'
save_dir = './results/phantom2_noise/anls'

save_file = '{name}_{feat}_{lambda_w}_{lambda_h}'.format(
    name=save_name,
    feat=features,
    lambda_w=lambda_w,
    lambda_h=lambda_h
    )
